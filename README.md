# Free Proxy Scraper

A web application that scrapes free proxies from multiple sources and tests their availability.

## Features
- Scrapes proxies from multiple free sources
- Tests proxy connectivity in real-time
- Copy all working proxies with one click
- Modern responsive UI

## Deployment on Render

This app is configured for easy deployment on Render.com

## Local Development
```bash
npm install
npm start
Visit http://localhost:3000
Name: proxy-scraper (atau nama yang diinginkan)

Environment: Node

Region: Singapore (atau terdekat)

Branch: main

Root Directory: (kosongkan)

Build Command: npm install

Start Command: npm start

## 🔧 Step Deployment di Render

### **Step 1: Push ke GitHub**
```bash
# Initialize git repository
git init
git add .
git commit -m "Initial commit: Proxy Scraper App"

# Create GitHub repository dan push
git branch -M main
git remote add origin https://github.com/username/proxy-scraper.git
git push -u origin main
